import ProfileSetup from "./ProfileSetup"
export default function ProfilePage() {
  // For MVP, reuse the same form; in later tasks, we will add replace/download controls emphasis
  return <ProfileSetup />
}
